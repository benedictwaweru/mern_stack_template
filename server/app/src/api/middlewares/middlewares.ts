/**
 * Prepares or modifies requests before reaching the controller, or modifies responses.
 * Highly reusable across multiple routes or modules.
 * Runs before (or sometimes after) controllers
 * Authentication, logging, validation, error handling.
 * Invoked automatically for every request it’s attached to.
 */
