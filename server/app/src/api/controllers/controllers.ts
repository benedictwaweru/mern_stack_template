/**
 * Handles the actual business logic of an API endpoint
 * Specific to a route or module; not as reusable.
 * Processes the request after middleware execution.
 * Invoked only when the route matches the request and middleware chain passes control.
 */

export class UserController {}
